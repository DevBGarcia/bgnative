import { StyleSheet } from "react-native";

export const GlobalStyles = StyleSheet.create({
  screenContainer: {
    flex: 1,
    alignItems: "center",
    padding: 24,
  },
  backButtonStyle: {
    alignSelf: "flex-start",
  },
});
